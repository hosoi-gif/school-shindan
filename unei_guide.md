# スクール診断アクティビティ 運営メモ

## 概要

「スクール診断」は、全12問にノリよく直感で答えると、10種類のふざけ気味な診断タイプ（例：「ひらめき番長」「静かなる軍師」「異世界建国主」など）のいずれかが判定される、スマホ・PCで動く診断ツールです。会場ごとに発行されるURLにアクセスして遊ぶWebアプリになっている。

内部のロジックは、キャリア心理学で実際に使われている**RIASEC（ホランド理論）**をベースにしています。R(現実的)・I(研究的)・A(芸術的)・S(社会的)・E(企業的)・C(慣習的)という6つの興味タイプを12問で測定し、各スクールコースに設定した「主軸・副軸」のRIASECプロファイルとの一致度でおすすめコースを決めます（詳細は下記「スコアリングの仕組み」参照）。単に選択肢とコースを1対1対応させる方式より、回答の裏付けがしっかりした設計です。

タイプは5つのスクールコース（iPhoneアプリ／Androidアプリ／Unityゲーム／Webデザイン／Webサービス）それぞれに「熱血系」「クール系」の2フレーバーがある構成（5コース×2＝10タイプ）。結果画面には以下が表示されます。

1. 診断タイプ名・ひとことキャッチコピー・タイプ説明
2. おすすめコースの紹介文
3. コース別「適性データ」%グラフ（5コース分。RIASECの回答傾向から自動計算）
4. 先輩作品ギャラリー（スワイプで見られるカルーセル。※現在はプレースホルダー）

## 使う前の準備

- このツールは今はWebサーバーとして動いている（USB配布やファイルのダブルクリックは不要）。生徒には**会場ごとの参加者URL**を配る（Wi-Fi案内やスライドにURL・QRコードを載せるなど）。
- 内容の編集はコードを直接いじらず、**最上位管理画面（`/admin`）から行う**。パスワードを入力すると、質問・コース・タイプ・ギャラリー・QRコードなどをフォームで編集でき、「この内容を保存する」を押すとその場でサーバーに反映される（全会場共通）。
- 先輩作品ギャラリーの差し替え（重要）：最上位管理画面の各コースの「先輩作品ギャラリー」欄で、種類を「画像URL」または「動画URL」に切り替えて実際のURLを入力すると、プレースホルダーの絵文字から本物の写真・動画（YouTubeは自動で埋め込み再生になる）に差し替わる。
- 結果画面下部の「QRコード貼り付け」枠も、最上位管理画面の「スタート画面・CTA文言」欄でQRコード画像URL・申込ページURLを入力すれば反映される。両方空欄なら従来通りプレースホルダー表示のまま。
- 会場ごとのURL発行の手順は、下の「会場ごとに『笑える質問』を混ぜたい場合」を参照。

## 進行の流れ（目安5〜10分）

1. （1分）「実は"つくるタイプ"にもいろいろあるんだよ、ちょっとふざけた診断だけどやってみて」と一言添えて配布。
2. （3〜4分）各自スマホ・PCで診断（全12問、直感で回答）。
3. （2〜3分）結果が出たら近くの子と「なにタイプだった？」「適性グラフ何%だった？」と見せ合う時間を取る。ふざけたタイプ名がネタになって盛り上がりやすい設計。
4. （1〜2分）先輩作品ギャラリーをスワイプしてもらい、「気になったコースがあった人はあとでスタッフに声をかけてね」と入塾窓口へ自然に接続。

## 使うタイミングの案

- 昼休みや移動前後などの「すきま時間」
- 1日のプログラム終了後の振り返り前後
- 最終日、修了証を渡す前後（達成感とセットで「もっと続けたい」気持ちを喚起しやすい）

## 一言スクリプト例

「今日ここで少しだけ触れた"つくる"を、実はライフイズテック スクールでは1年間かけてじっくり続けられるんだ。さっきの診断で気になったコースがあった人、この後スタッフに聞いてみてね！」

## スコアリングの仕組み（カスタマイズ時の参考）

このツールはRIASEC（ホランド理論）という、実際のキャリア心理学・進路適性診断で使われている枠組みを土台にしています。

1. **設問**：全12問、各設問に6つの選択肢（R:現実的／I:研究的／A:芸術的／S:社会的／E:企業的／C:慣習的、必ずこの順）。選んだ選択肢の分だけ、その興味タイプのスコアが+1される（12問で合計12点を6タイプに配分）。
2. **コースとの紐付け**：各スクールコースに「主軸（2点の重み）」「副軸（1点の重み）」というRIASECプロファイルを設定済み。
   - iPhoneアプリ：I（研究的）＋R（現実的）
   - Androidアプリ：C（慣習的）＋I（研究的）
   - Unityゲーム：A（芸術的）＋E（企業的）
   - Webデザイン：A（芸術的）＋C（慣習的）
   - Webサービス：S（社会的）＋E（企業的）
3. **コース別スコア**：回答者の各タイプの得点 × 該当コースの重み、を足し合わせた「内積」で算出。最も高いコースがおすすめコースになる。
4. **適性%グラフ**：コース別スコア ÷（主軸の重み2 × 設問数12）を基本に、最低5%・最大99%でクリップして表示。
5. **診断タイプの熱血/クールの決定**：E＋S（対人・行動力寄り）の合計と、C＋I（分析・秩序寄り）の合計を比べ、E＋Sの方が高ければ「熱血系」、そうでなければ「クール系」。

この仕組みにより「なんとなくコースを選んだ結果」ではなく、6つの興味タイプの測定結果からコースとの一致度を計算する、理屈の通った適性診断になっています。

## カスタマイズしたい場合

コードは直接編集せず、**最上位管理画面（`/admin`）を開いて編集する**。パスワードを入れると以下がフォームで編集できる。

- 質問文・選択肢（各設問6択、R→I→A→S→E→Cの順は固定）。質問の追加・削除も可能。全会場共通。
- コースとRIASECの重み付け（0〜3の数値）。コースの追加・削除も可能。ただし**コースを追加する場合は、そのコース専用の熱血/クールタイプ名・キャッチコピー・説明文を自分で入力する必要がある**（自動生成はされない）。
- コース紹介文、診断タイプ（熱血/クールそれぞれ）の名前・キャッチコピー・説明文・キャラ画像URL。
- 先輩作品ギャラリー（コースごと3件、画像URL・動画URLの差し替えも含む）。
- スタート画面の文言・結果画面下部のCTA文言・QRコード/申込URL。

編集が終わったら「この内容を保存する」を押す。全会場に即反映される（会場ごとのボーナス質問は別画面で設定、下記参照）。

## 会場ごとにURLを発行し、「笑える質問」を混ぜたい場合

診断とは無関係な、その会場だけのお楽しみ質問（例：「担任の先生の好きな給食は？」など）を挟み込める。診断結果には一切影響しない。

1. 最上位管理画面の「会場設定画面へ →」を押して会場設定画面（`/admin/venues`）を開く。
2. 「＋ 会場を追加」で会場名（例：体育館、教室Aなど）を入力して作成すると、その会場の「設定URL」と「参加者URL」が発行される。
3. 「設定URL」を、その会場を担当するスタッフに渡す（LINEやSlackで送るなど）。最上位管理画面のパスワードを教える必要はない — このURLを開くだけで、その会場専用の「会場オリジナル設定画面」に入れる。
4. 会場オリジナル設定画面で「＋ ボーナス質問を追加」し、質問文・選択肢（自由な数）・挿入位置（0=最初の質問より前、Nを指定すると共通のN問目の直後）を入力して「保存する」。
5. 同じ画面に表示される「参加者URL」を、その会場の生徒に配る（QRコードにして掲示するのがおすすめ）。
6. 会場ごとの設定は完全に独立している。ある会場のボーナス質問を編集・追加・削除しても、他の会場の参加者URLやボーナス質問には一切影響しない。
